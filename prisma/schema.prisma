generator client {
  provider     = "prisma-client-js"
  engineType   = "library"
  moduleFormat = "esm"
  output       = "../src/pg"
}

datasource db {
  provider = "postgresql"
}

model Example {
  id   String @id @unique
  text String
}
